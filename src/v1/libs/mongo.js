export let errs = {
    "properties": {
        status: 400, message: "lo sentimos algunas propiedades son incorrectas!"
    },
    "required": {
        status: 400, message: "hay campos que deben ser llenados!"
    }
}
